export function transcribeAudio(audio) {
    // const fs = require('fs');
    // const { Deepgram } = require('@deepgram/sdk');
    // require('dotenv').config();
    // const deepgramApiKey = process.env.API_DEEPGRAM;
    // // const file = 'YOUR_FILE_LOCATION';
    // // let mimetype = 'audio/*';
    // let deepgram = new Deepgram(deepgramApiKey);
    // // const audio = fs.readFileSync(file);

    // console.log('transcribeAudio');

    // source = {
    //     url: audio
    // }

    // deepgram.transcription.preRecorded(
    // source,
    // {
    //     punctuate: true
    // }
    // )
    // .then((response) => {
    // console.dir(response, {depth: null});

    // console.dir(response.results.channels[0].alternatives[0].transcript, { depth: null });
    // })
    // .catch((err) => {
    // console.log(err);
    // })
};